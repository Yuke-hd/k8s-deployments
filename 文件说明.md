# 文件说明

这个目录保存的是当前 Home Assistant 在 Kubernetes 里的部署清单和文档。

## 每个文件的作用

- `homeassistant-namespace.yaml`：创建 `homeassistant` 命名空间，用来隔离 Home Assistant 相关的命名空间级资源。
- `homeassistant-pv.yaml`：定义底层 `PersistentVolume`，后端使用 NFS，负责真正保存 Home Assistant 的持久化数据。
- `homeassistant-pvc.yaml`：定义 `PersistentVolumeClaim`，让 `homeassistant` 命名空间里的 Pod 可以申请并挂载上面的持久卷。
- `homeassistant-app.yaml`：定义 Home Assistant 的 `Deployment`，包括镜像、探针、环境变量、卷挂载，以及调度规则。
- `homeassistant-service.yaml`：定义集群内的 `ClusterIP Service`，把对 Home Assistant 的访问统一暴露在 `8123` 端口上。
- `ingressroute.yaml`：定义 Traefik 的 `IngressRoute`，把 `homeassistant-dev.lan` 这个域名转发到 Home Assistant 的 Service。
- `HA-setup-draft.md`：当前架构说明文档草稿，包含架构图、请求流程、failover 说明和 vote-only node 相关背景。
- `README.md`：英文目录说明，介绍每个文件的用途和建议的部署顺序。
- `LICENSE`：仓库许可证文件。

## 推荐理解顺序

如果是第一次看这个目录，建议按下面顺序理解：

1. `homeassistant-namespace.yaml`
2. `homeassistant-pv.yaml`
3. `homeassistant-pvc.yaml`
4. `homeassistant-app.yaml`
5. `homeassistant-service.yaml`
6. `ingressroute.yaml`

这样会比较容易从“资源准备”一路看到“应用部署”和“入口暴露”的完整链路。
